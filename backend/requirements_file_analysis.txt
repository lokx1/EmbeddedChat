# Additional requirements for file analysis functionality
# Add these to your existing requirements.txt or install separately

# PDF processing
PyPDF2>=3.0.1
PyMuPDF>=1.23.0  # fitz

# Image processing
Pillow>=10.0.0

# Office documents
python-docx>=0.8.11
openpyxl>=3.1.0

# AI providers (if not already installed)
openai>=1.0.0
anthropic>=0.25.0
# Google AI for document, image, video, audio processing
google-genai>=0.2.0

# File type detection
python-magic>=0.4.27  # Optional, for better MIME type detection
